import { Renderer, ElementRef, QueryList } from '@angular/core';
/**
 * Shared directive to count lines inside a text area, such as a list item.
 * Line elements can be extracted with a @ContentChildren(VdlLine) query, then
 * counted by checking the query list's length.
 */
export declare class VdlLine {
}
/**
 * Helper that takes a query list of lines and sets the correct class on the host.
 * @docs-private
 */
export declare class VdlLineSetter {
    private _lines;
    private _renderer;
    private _element;
    constructor(_lines: QueryList<VdlLine>, _renderer: Renderer, _element: ElementRef);
    private _setLineClass(count);
    private _resetClasses();
    private _setClass(className, bool);
}
export declare class VdlLineModule {
}
