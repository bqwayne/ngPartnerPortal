import { ElementRef } from '@angular/core';
import { VdlInkBar } from '../ink-bar';
/**
 * Navigation component matching the styles of the tab group header.
 * Provides anchored navigation with animated ink bar.
 */
export declare class VdlTabNavBar {
    _inkBar: VdlInkBar;
    /** Animates the ink bar to the position of the active link element. */
    updateActiveLink(element: HTMLElement): void;
}
/**
 * Link inside of a `vdl-tab-nav-bar`.
 */
export declare class VdlTabLink {
    private _vdlTabNavBar;
    private _element;
    private _isActive;
    /** Whether the link is active. */
    active: boolean;
    constructor(_vdlTabNavBar: VdlTabNavBar, _element: ElementRef);
}
