import { EventEmitter, OnInit, OnDestroy, Injector, Renderer, ElementRef, TemplateRef, ViewContainerRef, ComponentFactoryResolver, NgZone } from '@angular/core';
import { VdlPopoverConfig } from './popover-config';
export { VdlPopoverConfig } from './popover-config';
export declare class VdlPopoverWindow {
    placement: 'auto' | 'top' | 'bottom' | 'left' | 'right';
    title: string;
}
/**
 * A lightweight, extensible directive for fancy popover creation.
 */
export declare class VdlPopover implements OnInit, OnDestroy {
    private _elementRef;
    private _renderer;
    /**
     * Content to be displayed as popover.
     */
    vdlPopover: string | TemplateRef<any>;
    /**
     * Title of a popover.
     */
    popoverTitle: string;
    /**
     * Width of popover.
     */
    popoverWidth: number;
    /**
     * Height of popover.
     */
    popoverHeight: number;
    /**
     * Placement of a popover. Accepts: "auto", "top", "bottom", "left", "right"
     */
    placement: 'auto' | 'top' | 'bottom' | 'left' | 'right';
    /**
     * Specifies events that should trigger. Supports a space separated list of event names.
     */
    triggers: string;
    /**
     * A selector specifying the element the popover should be appended to.
     * Currently only supports "body".
     */
    container: string;
    /**
     * Emits an event when the popover is shown
     */
    shown: EventEmitter<{}>;
    /**
     * Emits an event when the popover is hidden
     */
    hidden: EventEmitter<{}>;
    private _popupService;
    private _windowRef;
    private _unregisterListenersFn;
    private _zoneSubscription;
    /**
     * Set to `true` after the popover opens; used to cancel the document click
     * handler so it doesn't immediately close the popover.
     */
    private _triggered;
    constructor(_elementRef: ElementRef, _renderer: Renderer, injector: Injector, componentFactoryResolver: ComponentFactoryResolver, viewContainerRef: ViewContainerRef, config: VdlPopoverConfig, ngZone: NgZone);
    _handleClickOutsidePopover(e: Event): false;
    /**
     * Opens an element’s popover. This is considered a “manual” triggering of the popover.
     */
    open(context?: any): void;
    /**
     * Closes an element’s popover. This is considered a “manual” triggering of the popover.
     */
    close(): void;
    /**
     * Toggles an element’s popover. This is considered a “manual” triggering of the popover.
     */
    toggle(): void;
    /**
     * Returns whether or not the popover is currently being shown
     */
    isOpen(): boolean;
    ngOnInit(): void;
    ngOnDestroy(): void;
}
