import { VdlIconRegistry } from '../icon/icon-registry';
export declare class VdlPopoverPredefined {
    popoverTitle: string;
    popoverPlacement: 'auto' | 'top' | 'bottom' | 'left' | 'right';
    popoverType: 'info' | 'error' | 'custom';
    popoverIcon: string;
    constructor(vdlIconRegistry: VdlIconRegistry);
}
