import { Injector, TemplateRef } from '@angular/core';
import { Observable } from 'rxjs/Observable';
import { Subject } from 'rxjs/Subject';
import { Overlay, ComponentType } from '../core';
import { VdlDialogConfig } from './dialog-config';
import { VdlDialogRef } from './dialog-ref';
/**
 * Service to open Material Design modal dialogs.
 */
export declare class VdlDialog {
    private _overlay;
    private _injector;
    private _parentDialog;
    private _openDialogsAtThisLevel;
    private _afterAllClosedAtThisLevel;
    private _afterOpenAtThisLevel;
    /** Keeps track of the currently-open dialogs. */
    readonly _openDialogs: VdlDialogRef<any>[];
    /** Subject for notifying the user that all open dialogs have finished closing. */
    readonly _afterOpen: Subject<VdlDialogRef<any>>;
    /** Subject for notifying the user that a dialog has opened. */
    readonly _afterAllClosed: Subject<void>;
    /** Gets an observable that is notified when a dialog has been opened. */
    afterOpen: Observable<VdlDialogRef<any>>;
    /** Gets an observable that is notified when all open dialog have finished closing. */
    afterAllClosed: Observable<void>;
    constructor(_overlay: Overlay, _injector: Injector, _parentDialog: VdlDialog);
    /**
     * Opens a modal dialog containing the given component.
     * @param componentOrTemplateRef Type of the component to load into the dialog,
     *     or a TemplateRef to instantiate as the dialog content.
     * @param config Extra configuration options.
     * @returns Reference to the newly-opened dialog.
     */
    open<T>(componentOrTemplateRef: ComponentType<T> | TemplateRef<T>, config?: VdlDialogConfig): VdlDialogRef<T>;
    /**
     * Closes all of the currently-open dialogs.
     */
    closeAll(): void;
    /**
     * Creates the overlay into which the dialog will be loaded.
     * @param dialogConfig The dialog configuration.
     * @returns A promise resolving to the OverlayRef for the created overlay.
     */
    private _createOverlay(dialogConfig);
    /**
     * Attaches an VdlDialogContainer to a dialog's already-created overlay.
     * @param overlay Reference to the dialog's underlying overlay.
     * @param config The dialog configuration.
     * @returns A promise resolving to a ComponentRef for the attached container.
     */
    private _attachDialogContainer(overlay, config);
    /**
     * Attaches the user-provided component to the already-created VdlDialogContainer.
     * @param componentOrTemplateRef The type of component being loaded into the dialog,
     *     or a TemplateRef to instantiate as the content.
     * @param dialogContainer Reference to the wrapping VdlDialogContainer.
     * @param overlayRef Reference to the overlay in which the dialog resides.
     * @param config The dialog configuration.
     * @returns A promise resolving to the VdlDialogRef that should be returned to the user.
     */
    private _attachDialogContent<T>(componentOrTemplateRef, dialogContainer, overlayRef, config?);
    /**
     * Creates an overlay state from a dialog config.
     * @param dialogConfig The dialog configuration.
     * @returns The overlay configuration.
     */
    private _getOverlayState(dialogConfig);
    /**
     * Removes a dialog from the array of open dialogs.
     * @param dialogRef Dialog to be removed.
     */
    private _removeOpenDialog(dialogRef);
}
