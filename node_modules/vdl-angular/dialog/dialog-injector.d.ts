import { Injector, OpaqueToken } from '@angular/core';
import { VdlDialogRef } from './dialog-ref';
export declare const VDL_DIALOG_DATA: OpaqueToken;
/** Custom injector type specifically for instantiating components with a dialog. */
export declare class DialogInjector implements Injector {
    private _parentInjector;
    private _dialogRef;
    private _data;
    constructor(_parentInjector: Injector, _dialogRef: VdlDialogRef<any>, _data: any);
    get(token: any, notFoundValue?: any): any;
}
