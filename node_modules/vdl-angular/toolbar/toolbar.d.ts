import { ModuleWithProviders, ElementRef, Renderer } from '@angular/core';
export declare class VdlToolbarRow {
}
export declare class VdlToolbar {
    private elementRef;
    private renderer;
    private _color;
    constructor(elementRef: ElementRef, renderer: Renderer);
    /** The color of the toolbar. Can be primary, accent, or warn. */
    color: string;
    private _updateColor(newColor);
    private _setElementColor(color, isAdd);
}
export declare class VdlToolbarModule {
    /** @deprecated */
    static forRoot(): ModuleWithProviders;
}
